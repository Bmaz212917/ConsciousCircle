<resources>
    <style name="AppTheme" parent="Theme.AppCompat.DayNight.NoActionBar">
        <!-- Customize your theme here. -->
        <!-- <item name="android:editTextBackground">@drawable/rn_edit_text_material</item> -->
    </style>

    <style name="BootTheme" parent="Theme.BootSplash">
        <!-- Set the background image for the splash screen -->
        <item name="android:windowBackground">@drawable/splash</item> <!-- Fullscreen background image -->

        <!-- Set the splash logo with appropriate scale type -->
        <!-- <item name="bootSplashLogo">@drawable/splash</item> Use the same image for logo -->

        <!-- Optional: Set the theme after the splash screen -->
        <item name="postBootSplashTheme">@style/AppTheme</item>
    </style>
</resources>
