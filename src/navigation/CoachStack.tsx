import {createStackNavigator} from '@react-navigation/stack';

const Stack = createStackNavigator();

const CoachStack = () => {
  return (
    <Stack.Navigator>
      {/* <Stack.Screen name="CoachHome" component={HomeScreen} /> */}
    </Stack.Navigator>
  );
};

export default CoachStack;
